# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Sync-ing with GitHub repo!
  displayName: 'Start sync-ing branches'

- script: 'git push github --mirror'
  displayName: 'mirror changes to github'
